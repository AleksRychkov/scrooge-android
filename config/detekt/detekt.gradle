apply plugin: "io.gitlab.arturbosch.detekt"

detekt {
    config.from(rootProject.files("config/detekt/detekt.yml"))
    buildUponDefaultConfig = true
    parallel = true
}

dependencies {
    detektPlugins libs.detekt.formatting
    detektPlugins(project(":config:detekt:rules"))
}
